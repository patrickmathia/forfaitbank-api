/**
 * Maximum bill quantity that a single package can hold.
 *
 * @const
 */
export const MaxBillQuantity = 50

/**
 * Maximum value that a single operation can hold.
 *
 * @const
 */
export const MaxOperationValue = 5000

